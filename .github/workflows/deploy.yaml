name: GitHub Actions Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ls ./public
      - run: aws s3 sync ./public/ s3://petergeorgantas.com/dev/
